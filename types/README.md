# types/

TypeScript type definitions shared across the application.

## Contents

| File | Purpose |
|------|---------|
| `index.ts` | All shared TypeScript types exported for use by `app/`, `components/`, and `lib/` |

Key types include `FireEvent`, `EvacueeRecord`, role types, and API response shapes. See the file directly for the full type catalog.
