// LanguageModal is no longer used — language defaults to English on first login.
// Users change language via the sidebar Globe button or the Profile page.
export default function LanguageModal() { return null }
